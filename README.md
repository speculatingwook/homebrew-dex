# homebrew-dex

Homebrew tap for [dex](https://github.com/speculatingwook/dex) — a side-by-side document editor and terminal for AI-assisted development.

## Install

```bash
brew tap speculatingwook/dex
brew install --cask dex
```

Or in one command:

```bash
brew install --cask speculatingwook/dex/dex
```

## Update

```bash
brew upgrade --cask dex
```

## Uninstall

```bash
brew uninstall --cask dex
brew untap speculatingwook/dex
```
